# 核心开发原则与防腐败机制

## 1. 架构规范（绝对红线）：
* **禁止上帝对象（No God Object）**：`main.py` 及插件主入口类仅能作为装配站（IOC）与事件路由，绝对禁止将具体的业务控制流、数据状态缓存、特定的定时任务，或者长篇大论的 if-else 命令派发直接写在主入口内。
* **依赖注入解耦**：当拆分子模块（如 Handler、Service、Manager）时，严禁将整个 Plugin 实例（self）作为核心参数胡乱下发。子模块只允许接收它实际需要的具体对象（如特定的 Config、API Client 或 Repository）。**所有依赖必须在其类实例化的 `__init__` 方法中显式声明并传入，严禁通过定义全局字典、单例模式或方法内局部实例化来绕过依赖验证。**
* **拒绝魔法数字与硬编码**：任何特定的时间阈值、特定的资源 ID（如 QQ 表情 ID）、正则匹配 pattern 等，必须统一提取配置类或常数管理模块，严禁将无意义的数字、字符串直接散落和硬编码在业务逻辑中。

## 2. 状态与并发管理（绝对红线）：
* 如果插件涉及复杂状态持久化或高并发频繁读写（如消息日志、刷屏检测、多群配置），**严禁使用简陋的“内存字典 + 定时整体覆写 JSON + 满天飞的 asyncio.Lock”**。
* 必须向我提议使用 SQLite (aiosqlite) 等真正的轻量级关系型数据库，或严格落实封装 Repository 仓储模式，彻底杜绝在业务流代码中散落显式的底层加锁操作。
* **严禁阻塞事件循环**：在 `async def` 声明的事件处理函数内，绝对不允许出现任何同步阻塞代码（如 `requests.get`、`time.sleep` 或同步 `open()`）。必须严格使用 `aiohttp`、`asyncio.sleep` 及 `aiofiles` 等异步替代方案。

## 3. 代码质量与健壮性：
* **异常处理与日志收口**：所有的外部网络 API 请求、文件 I/O 必须包含完善的 `try-except` 异常捕获机制。错误必须被妥善处理并使用 AstrBot 插件标准的 logger（如 `logger.error`）输出，**严禁在生产和重构环境使用 `print()` 调试**。绝不允许让未捕获的异常导致主进程崩溃卡死。严禁使用外部重启或“定时清理死锁”之类的野路子掩盖代码设计缺陷。
* **类型推断与依赖管理**：必须执行严格的 Python Type Hinting (PEP 484)，函数签名必须标明入参与返回值类型。如需引入系统级第三方依赖（如 pip 包），必须在实施前向我申请并说明不可替代的理由，严禁擅自引入。**一旦获准引入新的 pip 依赖，必须同步将包名及版本要求追加到插件根目录下的 `requirements.txt` 中，并且若涉及平台兼容性变更，须主动提醒我更新 `metadata.yaml`。否则坚决不予验收，防止用户安装插件时出现致命崩溃。**
* **编码约束**：代码文件严格使用 UTF-8 编码。输出的代码风格必须完全符合 Ruff (PEP 8) 标准，严禁输出未使用的包导入（Unused imports）和冗余空行。终端日志、接口回复和核心注释推荐使用规范且符合国内环境的中文，不需要退回到纯 ASCII 设置。

## 4. 沟通与改动依据：
* 全程的代码改动必须有理有据，不进行偏离当前任务的过度重构或炫技。
* 但如果在当前需求下，某个类或单文件有超过 600-800 行的趋势，或明确破坏了单一职责原则，你必须主动中断开发，先向我提出模块化拆分的建议。
* 保持专业严谨，直接输出有价值的代码和判断结论。在与我沟通的反馈文本中绝对**禁止使用任何 Emoji**（注：不限制实际业务逻辑代码的载荷）。遇到 AstrBot API 疑惑，优先查阅 @https://docs.astrbot.app/dev/star/plugin.html ，禁止脑补原型。
* **文档防腐机制（Single Source of Truth，绝对红线）**：严禁在 `ChangeLogs/` 或根目录下每次改动都新建分散的 `.txt` 或文件夹。
  1. 改动前，你必须**唯一**阅读根目录下的 `ARCHITECTURE.md`（或 `TODO.md`）获取当前进度与技术债。若产生新的重构计划，必须且只能对该文件进行**就地覆写更新**。如果这两个文件夹没有存在，那就在文件夹的目录下直接创建。
  2. 每次开发完成并通过我的验收后，严格按照 Keep a Changelog 规范，将本次变更追加到根目录的 `CHANGELOG.md` 中。
* **版本控制规范**：此项目已全面接入 Git。在常规开发中，你只需就地修改代码即可。当且仅当我明确下达“验收通过并提交”之类的指令时，你才能接入执行相关的 Git CLI 提交流程。日常重构时严禁擅自手动创建零散的 `.bak`、`.old` 等垃圾备份文件。

## 5. 交互与验收工作流 (严格执行)：
每次对一个功能进行创建或改动时，必须按顺序执行以下步骤：
1. **架构与变更汇报**：清晰且简短地告诉我改动了哪些文件、为什么要这样改。如果是新增模块，必须说明它的边界在哪，数据流和生命周期是怎样的。
2. **代码输出约束**：只输出发生变更的核心代码块或清晰的 Diff 行。不要在微小改动时无脑重绘输出动辄上百行的完整文件。
3. **验收方案**：提供对应的测试前置条件、并发边界条件预期，说明我应该如何验证该功能以确保不崩溃。
4. **强制等待（Hold）**：汇报完成并提供上述内容后，必须立即停止输出，等待我的验收反馈确认。只有在我明确回复“验收通过”或指出修正点后，你才能进入下一个迭代开发。
5. **阻断死循环**：如果生成的代码在执行时引发异常（Traceback），禁止未取得我同意前连续尝试两次以上的盲目修补。必须立即停止并汇报完整堆栈及分析，等待我的决断。
6. **验收后自动化交付与容灾备份（执行阶段）**：
   当我明确回复下达“验收通过，直接提交”级别的明确指令时，你必须使用终端工具按以下流程自动化交付代码，**非此情境下严禁自行操作 Git**：
   - **步骤 A (Commit)**：提取本次的修改核心总结作为 commit message，执行 `git add .` 与 `git commit -m "变更说明"`。如有需要，同步将其记录在 `CHANGELOG.md` 中。
   - **步骤 B (Push)**：执行 `git push` 推送到远程 GitHub 仓库。
   - **步骤 C (容灾备份Fallback)**：若 `git push` 遇到任何终端错误（如网络超时、连接断开或冲突未解决），**禁止强制覆盖或陷入死循环重试**。你必须立即在当前插件目录下通过终端 PowerShell 命令创建一个名为 `backups` 的文件夹（若不存在），将当前工作目录的核心改动打包（或建立按当天时间戳命名的子文件夹，例如 `backups/20260409_1530/`）在本地留存备份，并向我汇报推送失败及备份完成情况。

## 6. 面向 AstrBot 的特化防腐规范（绝对红线）：

* **严禁“反向依赖”与回调泥潭**：
  主入口（`main.py`）必须是严格的“顶层调用者”。底层子服务（如各种 `Service` 或 `Handler`）严禁通过接收外部传递的 callback/回调函数来反向调用 `main.py` 的私有方法。如果某个业务逻辑需要拆分，必须是主类显式调用子模块的方法并获取返回值（如直接返回 `MessageEventResult`），绝对不允许子模块把逻辑抛回给老父亲执行。

* **严格的数据持久化路径（防止更新丢失）**：
  根据 AstrBot 官方文档规范，插件的持久化数据（如 SQLite 数据库、各类状态 JSON）必须存储在机器人根目录独立的 `data/` 文件夹下（例如构建路径：`os.path.join(os.getcwd(), "data", "你的插件名")` ）。**严禁**使用 `__file__.parent` 将数据文件写在插件源码所在的同级物理目录内。否则任何基于 WebUI 的一键更新或重新安装都会导致群数据被彻底抹除。

* **强制使用原生事件路由与统一消息载体**：
  必须且只能使用 AstrBot 官方提供的 `@filter.command()`、`@filter.on_message()` 等事件过滤装饰器进行命令与事件的分发。严禁自己手动拦截底层消息事件后暴力拆解字符串写土制路由。在构造交互消息时，必须使用标准的 `MessageEventResult` 机制（如 `yield event.plain_result()`），除非明确要求，严禁调用仅限于单一特定平台（如仅限 QQ）支持的特殊消息接口，确保跨平台兼容性优先。

* **热重载（Hot-Reloading）的优雅销毁与僵尸任务防范**：
  由于 AstrBot 支持在 WebUI 随时一键重载插件，如果插件内部启动了基于 `asyncio` 的独立后台任务（Task）、长连接或使用了数据库连接池，必须要求你提供或显式实现安全的退出策略与销毁监听逻辑。保证在重载时会主动释放连接和取消所有未决挂起的 Tasks。严禁因为用户点击“重载”，而在内存中挂起几十个死循环任务或产生连接泄漏。

* **杜绝“空壳转发”与过度设计**：
  不要为了表面的“解耦”，去创建仅仅用来把参数原封不动转抛给另外一个类的“空包裹类”。如果一个模块的某个方法内部只有一句 `return another_service.do_something()`，请立刻消灭这个中间层。保持架构扁平化，不要在 Python 异步脚本里生搬硬套 Java 的全套 DTO/VO 复杂企业级模式。
