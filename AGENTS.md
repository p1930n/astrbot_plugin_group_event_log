# 核心开发原则与防腐败机制

## 1. 架构规范（绝对红线）：
* **禁止上帝对象（No God Object）**：`main.py` 及插件主入口类仅能作为装配站（IOC）与事件路由，绝对禁止将具体的业务控制流、数据状态缓存、特定的定时任务，或者长篇大论的 if-else 命令派发直接写在主入口内。
* **依赖注入解耦**：当拆分子模块（如 Handler、Service、Manager）时，严禁将整个 Plugin 实例（self）作为核心参数胡乱下发。子模块只允许接收它实际需要的具体对象（如特定的 Config、API Client 或 Repository）。
* **拒绝魔法数字与硬编码**：任何特定的时间阈值、特定的资源 ID（如 QQ 表情 ID）、正则匹配 pattern 等，必须统一提取配置类或常数管理模块，严禁将无意义的数字、字符串直接散落和硬编码在业务逻辑中。

## 2. 状态与并发管理（绝对红线）：
* 如果插件涉及复杂状态持久化或高并发频繁读写（如消息日志、刷屏检测、多群配置），**严禁使用简陋的“内存字典 + 定时整体覆写 JSON + 满天飞的 asyncio.Lock”**。
* 必须向我提议使用 SQLite (aiosqlite) 等真正的轻量级关系型数据库，或严格落实封装 Repository 仓储模式，彻底杜绝在业务流代码中散落显式的底层加锁操作。

## 3. 代码质量与健壮性：
* **异常处理**：所有的外部网络 API 请求、文件 I/O 必须包含完善的 `try-except` 异常捕获机制。错误必须被妥善处理并输出带有上下文的标准日志，绝不允许让未捕获的异常导致主进程崩溃卡死。严禁使用外部重启或“定时清理死锁”之类的野路子掩盖代码设计缺陷。
* **类型推断与依赖**：必须执行严格的 Python Type Hinting (PEP 484)，函数签名必须标明入参与返回值类型。如需引入系统级外部依赖（如 npm/pip 包），必须在实施前向我申请并说明不可替代的理由，严禁擅自引入。
* **编码约束**：代码文件严格使用 UTF-8 编码。终端日志、接口回复和核心注释推荐使用规范且符合国内环境的中文，不需要退回到纯 ASCII 设置。

## 4. 沟通与改动依据：
* 全程的代码改动必须有理有据，不进行偏离当前任务的过度重构或炫技。
* 但如果在当前需求下，某个类或单文件有超过 600-800 行的趋势，或明确破坏了单一职责原则，你必须主动中断开发，先向我提出模块化拆分的建议。
* 保持专业严谨，直接输出有价值的代码和判断结论。在与我沟通的反馈文本中绝对**禁止使用任何 Emoji**（注：不限制实际业务逻辑代码的载荷）。遇到 AstrBot API 疑惑，优先查阅 @https://docs.astrbot.app/dev/star/plugin.html ，禁止脑补原型。
* **文档防腐机制（Single Source of Truth，绝对红线）**：严禁在 `ChangeLogs/` 或根目录下每次改动都新建分散的 `.txt` 或文件夹。
  1. 改动前，你必须**唯一**阅读根目录下的 `ARCHITECTURE.md`（或 `TODO.md`）获取当前进度与技术债。若产生新的重构计划，必须且只能对该文件进行**就地覆写更新**。
  2. 每次开发完成并通过我的验收后，严格按照 Keep a Changelog 规范，将本次变更追加到根目录的 `CHANGELOG.md` 中。

## 5. 交互与验收工作流 (严格执行)：
每次对一个功能进行创建或改动时，必须按顺序执行以下步骤：
1. **架构与变更汇报**：清晰且简短地告诉我改动了哪些文件、为什么要这样改。如果是新增模块，必须说明它的边界在哪，数据流和生命周期是怎样的。
2. **代码输出约束**：只输出发生变更的核心代码块或清晰的 Diff 行。不要在微小改动时无脑重绘输出动辄上百行的完整文件。
3. **验收方案**：提供对应的测试前置条件、并发边界条件预期，说明我应该如何验证该功能以确保不崩溃。
4. **强制等待（Hold）**：汇报完成并提供上述内容后，必须立即停止输出，等待我的验收反馈确认。只有在我明确回复“验收通过”或指出修正点后，你才能进入下一个迭代开发。
